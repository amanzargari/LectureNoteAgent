services:
  slideagent-web:
    build: .
    container_name: lecture-note-agent
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
